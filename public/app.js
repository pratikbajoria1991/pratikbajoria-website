const FALLBACK_POSTS=[{title:'The quiet advantage of a personal operating system',slug:'personal-operating-system',date:'2026-08-28',category:'Technology & leverage',readTime:6,image:'https://images.unsplash.com/photo-1497366811353-6870744d04b2?w=1200&q=85',excerpt:'The best productivity stack is not the one with the most tools. It is the one that turns good intentions into repeatable decisions.',url:'#'},{title:'A decision framework for buying your first investment property',slug:'first-investment-property',date:'2026-08-21',category:'Property & place',readTime:8,image:'https://images.unsplash.com/photo-1564013799919-ab600027ffc6?w=1200&q=85',excerpt:'A practical way to think about yield, leverage, vacancy, and the opportunity cost of your next rupee.',url:'#'},{title:'Compounding is a behaviour before it is a number',slug:'compounding-is-a-behaviour',date:'2026-08-14',category:'Capital allocation',readTime:5,image:'https://images.unsplash.com/photo-1611974789855-9c2a0a7236a3?w=1200&q=85',excerpt:'Why the most valuable financial habit is building a process that survives both optimism and fear.',url:'#'}];
function formatDate(value){return new Intl.DateTimeFormat('en-IN',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(value))}
function storyCard(post){const target=post.url&&post.url!=='#'?post.url:`#${post.slug}`;return `<article class="story-card"><a href="${target}" aria-label="Read ${post.title}"><div class="story-image"><img loading="lazy" src="${post.image}" alt="" /></div><div class="story-meta"><span>${post.category||'Field note'}</span><span>${formatDate(post.date)}</span></div><h3 class="story-title">${post.title}</h3><p class="story-excerpt">${post.excerpt||''}</p></a></article>`}
async function loadPosts(){try{const response=await fetch('/blog-posts.json',{cache:'no-store'});if(!response.ok)throw new Error('No posts file');const posts=await response.json();return Array.isArray(posts)&&posts.length?posts.slice(0,3):FALLBACK_POSTS}catch{return FALLBACK_POSTS}}
document.addEventListener('DOMContentLoaded',async()=>{document.querySelector('#year').textContent=new Date().getFullYear();const grid=document.querySelector('#featured-grid');const posts=await loadPosts();grid.innerHTML=posts.map(storyCard).join('')||'<p class="empty-state">The next dispatch is being edited.</p>';document.querySelector('#subscribe-form').addEventListener('submit',event=>{event.preventDefault();const status=document.querySelector('#form-status');status.textContent='You’re on the list — welcome to the long game.';status.classList.add('success');event.target.reset()})});

